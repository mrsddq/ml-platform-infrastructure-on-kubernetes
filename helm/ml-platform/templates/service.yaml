apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}-api
  labels:
    app.kubernetes.io/name: {{ .Chart.Name }}-api
spec:
  selector:
    app.kubernetes.io/name: {{ .Chart.Name }}-api
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
